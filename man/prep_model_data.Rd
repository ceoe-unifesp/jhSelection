% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{prep_model_data}
\alias{prep_model_data}
\title{Prepare the estimation sample for the reporting-bias model}
\usage{
prep_model_data(
  da_model,
  offset_var = "fe",
  remove_unknown = TRUE,
  cc = 0.5,
  drop_bench_zero = FALSE
)
}
\arguments{
\item{da_model}{Analysis data frame with columns \code{shr}, \code{fe}, \code{mpv}, \code{year},
\code{race}, \code{sex}, and a geographic identifier (\code{fips} and/or \code{state}).}

\item{offset_var}{Benchmark used as the offset, \code{"fe"} or \code{"mpv"}.}

\item{remove_unknown}{Drop \code{"Unknown/Others"} race and sex cells. Default \code{TRUE}.}

\item{cc}{Continuity correction applied to zero-benchmark cells. Default \code{0.5}.}

\item{drop_bench_zero}{If \code{TRUE}, drop cells whose benchmark is zero instead of
applying the continuity correction (a robustness check). Default \code{FALSE}.}
}
\value{
A prepared data frame with the offset column \code{ofs}.
}
\description{
Aligns the SHR outcome with a benchmark offset (FE or MPV), restricts to the
common 2013--2021 window, and applies the continuity correction for cells in
which the benchmark is zero. Factored out of \code{\link[=fit_model]{fit_model()}} so that the main
models and every robustness variant share exactly one data-preparation path.
}
\details{
The continuity correction replaces a zero (or missing) benchmark with \code{cc}
\strong{only} for cells where the SHR records at least one victim; it is never
added to the SHR outcome, and cells in which both the SHR and the benchmark
are zero are dropped. This mirrors the description in the paper's Empirical
Strategy section.
}
