% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidy.R
\name{build_da}
\alias{build_da}
\title{Build the county- and state-level analysis datasets}
\usage{
build_da(counts_shr, counts_fe, counts_mpv)
}
\arguments{
\item{counts_shr, counts_fe, counts_mpv}{Aggregated victim counts by
\verb{state, fips, year, race, sex}.}
}
\value{
A list with \code{fips} (county-year) and \code{state} (state-year) data frames.
}
\description{
Full-outer-joins the three aggregated count tables on
\verb{state, fips, year, race, sex}, sets the reference levels for \code{race} and
\code{sex}, and returns both the county-level table (missing SHR set to zero) and
the state-level aggregate. Factored out of \code{data-raw/6-tidy.R} so that the
Fatal Encounters robustness variants (alternative force filters and race
imputation) can be rebuilt through the same code path as the main data.
}
